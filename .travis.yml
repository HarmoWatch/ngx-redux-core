language: node_js

node_js:
  - stable

install:
  - npm install

script:
  - npm run ci:test
  - npm run ci:lint

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

addons:
  chrome: stable
